{
  "name": "@movensa/widget",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm build && vite preview --host 127.0.0.1 --port 4174",
    "preview": "vite preview --host 127.0.0.1 --port 4174",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vitest": "5.0.1"
  },
  "dependencies": {
    "@fontsource/poppins": "^5.3.0"
  }
}
